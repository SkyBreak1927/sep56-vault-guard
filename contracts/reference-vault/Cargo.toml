[package]
name = "reference-vault"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "26.1.0"
stellar-tokens = "0.7.2"

[dev-dependencies]
soroban-sdk = { version = "26.1.0", features = ["testutils"] }
